driver:
  name: docker

provisioner:
  name: chef_zero

verifier:
  name: inspec

platforms:
  - name: ubuntu

suites:
  - name: default
    run_list:
      - recipe[my_cookbook::default]
    attributes: {}
